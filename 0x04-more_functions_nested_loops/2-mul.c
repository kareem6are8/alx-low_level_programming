#include "main.h"

/**
 * mul - multiplies two integers
 * @x: The first number to multiply
 * @y: The second nember to multiply
 * Return: The result of multiplication betwen x param and y param
 */
int mul(int x, int y)

{
	return (x * y);
}
